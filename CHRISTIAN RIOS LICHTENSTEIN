// lala.cpp : Defines the entry point for the console application.
//autor: Christian Rios Lichtenstein
// fecha: 15/febrero/2017
#include "stdafx.h"  //Libreria del Visual C++ stdio
#include <iostream>  // Entrada y salida cin, cout
#include "conio.h"   //Para manejar la entrada y getch()
#include "math.h"    //manejar el pow
// Declara el teclado y el monitor como la salida y entrada estandard
using namespace std;

// El cuerpo principal del programa
void main()
{
   float area,base1,base,base2; // Declaración de variables
   // Ingreso de datos con mensaje y solicitud de datos
   cout<<"Ingrese base: ";
   cin>>base;
    if(base>0){
	cout<<"error" ; 
	} else 
   cout<<"Ingrese base2: ";
   cin>>base2;
    if(base2>0){
		cout<<"error" ; 
	} else 
   cout<<"Ingrese base3: ";
	 if(base2>0){
	} else 
   // Proceso: Calculo
   area=pow((base1+base2+base)/3+(base1+base2+base)/3-base)((base1+base2+base)/3-base1)((base1+base2+base)/3-base2)
   // Salida de la respuesta o resultado
   cout<<"El area es: "; 
   getch(); // Detiene la pantalla
}
